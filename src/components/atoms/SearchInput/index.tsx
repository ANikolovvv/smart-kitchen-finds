import React, { FC } from 'react';

type SearchProps = {
  isCollapsed: boolean;
  isDark: boolean;
};

const SearchInput: FC<SearchProps> = ({ isCollapsed, isDark }) => {
  // ... rest of component
};

export default SearchInput; 