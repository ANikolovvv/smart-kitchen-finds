import Box from "@/components/atoms/Box";
import React, { FC, memo } from "react";

const HomePage: FC = () => {
  return <Box className="h-full w-full max-h-screen"></Box>;
};

export default memo(HomePage);
